function Header() {
    return (
        <header className="navbar-dark bg-dark row justify-content-center mb-5">
            <a className="navbar-brand text-center" href="/">Achievement Tracker</a>
        </header>
    );
}

export default Header;